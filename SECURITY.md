# 安全策略

AperturePrism-AI-Review 处理 GitHub App 私钥、模型 Provider 凭据、OAuth 令牌等敏感信息，安全对我们至关重要。感谢你以负责任的方式报告安全问题。

## 报告漏洞

**请勿在公开 Issue 中披露安全漏洞。** 发现疑似漏洞时，请通过以下方式私密报告：

1. 在仓库「Security」页使用 **Report a vulnerability** 私密上报（推荐，报告会直接送达维护者）；
2. 或通过仓库主页 / 官网 <https://www.aprism.top> 的联系方式私下联系维护者。

报告请尽可能提供：受影响组件与版本、复现步骤、影响范围、建议修复。复现脚本或 PoC 有助于加速定位。

## 我们承诺

- **响应**：收到报告后 5 个工作日内确认并评估影响；
- **沟通**：修复期间与你保持沟通；
- **致谢**：修复并发布后，在你的同意下给予致谢。

## 受支持版本

我们仅对**最新发布版本**（及紧邻的上一版本）提供安全修复。旧版本请先升级到受支持版本后再报告。

## 范围

- **范围内**：本仓库源码、Docker 镜像打包、部署配置中的安全缺陷（认证绕过、密钥泄露、注入、越权、依赖漏洞等）。
- **范围外**：
  - **部署自伤**：未按文档部署导致的暴露（如生产环境不设 `WEBUI_API_TOKEN`、把管理接口直接暴露公网）——这属于配置问题而非代码缺陷；
  - **第三方依赖**：模型网关 / 中转站、QQ 协议端等外部组件自身的安全问题；
  - DoS / 社会工程类攻击。

## 安全最佳实践（部署者）

- 为 GitHub App 私钥、`CREDENTIAL_MASTER_KEY`、数据库密码使用独立随机值，绝不提交到仓库；
- 生产环境务必设置 `WEBUI_API_TOKEN`，并把管理接口置于内网或反向代理鉴权之后；
- 定期更新依赖并扫描漏洞：`npm audit`、启用 Dependabot / Renovate 自动升级、扫描 Docker 基础镜像（如 `docker scout` / Trivy）。
