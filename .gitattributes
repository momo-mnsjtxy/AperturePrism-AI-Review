# 规范文本/二进制识别，避免跨平台 CRLF 混入（Windows/SMB 环境尤甚）。
#
# 注：text=auto 只影响之后的 add/checkout，不会改写已入库 blob。若历史文件
# 已含 CRLF，需一次性 `git add --renormalize .` 收口（本仓库当前为 LF，无需执行）。

# 默认：文本自动规范化（仓库内统一 LF，checkout 按平台默认）
* text=auto

# 脚本必须 LF：Windows Git Bash 下也能稳定执行
*.sh text eol=lf

# 二进制文件不做任何转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.tgz binary
*.tar.gz binary

# 仅禁止换行转换，保留 diff 与 merge（base64 脚本等非文本产物）
*.b64 -text
